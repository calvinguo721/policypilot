name: Deploy
on:
  workflow_dispatch:
    inputs:
      cmd:
        description: 'Command'
        required: true
        default: 'echo ok'
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Execute
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: 121.43.67.216
        username: root
        password: 136204Aoc
        command_timeout: 120s
        script: ${{ github.event.inputs.cmd }}

name: Fix Daily Limit
on: workflow_dispatch
jobs:
  fix:
    runs-on: ubuntu-latest
    steps:
    - name: SSH and fix
      uses: appleboy/ssh-action@v1
      with:
        host: 121.43.67.216
        username: root
        password: 136204Aoc
        script: |
          sed -i 's/daily_limit = 3 if is_free/daily_limit = 1 if is_free/' /var/www/policypilot/engine/main.py
          sed -i 's/每日3次/每日1次/g' /var/www/policypilot/engine/main.py
          rm -rf /var/www/policypilot/engine/__pycache__
          systemctl restart policypilot
          echo done
